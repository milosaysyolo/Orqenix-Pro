## Summary

Automated fix for `ERR_PNPM_OUTDATED_LOCKFILE` in CI.

This PR regenerates `pnpm-lock.yaml` to match the current root `package.json`. No source code, workspace `package.json` files, or `.npmrc` settings were modified.

## Drift Detected

See `check-lockfile-drift.mjs` output in workflow logs.

## Validation Performed

- [x] `pnpm install --lockfile-only` completed without errors
- [x] `pnpm install --frozen-lockfile --lockfile-only` now passes
- [x] `pnpm.onlyBuiltDependencies` allowlist preserved (`better-sqlite3`, `esbuild`, `@swc/core`)
- [x] `.npmrc` not modified (`ignore-scripts=true` intact)
- [x] Diff scope: `pnpm-lock.yaml` only

## Reviewer Checklist

- [ ] Confirm the drifted specifiers match recent intentional changes to root `package.json`
- [ ] Confirm CI jobs pass (lockfile-guard, build, test)

## Generated By

`release-validator` agent, task `fix-lockfile-drift`.
