# propose-bump

Propose semver bump type per package from classifications.

## Input
- classifications: from classify-changes
- policyPath: path to release-policy.yaml
- forceBump: override all (optional)

## Output
Per-package bump proposals with reason.
