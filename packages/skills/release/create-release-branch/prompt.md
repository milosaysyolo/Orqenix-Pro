# create-release-branch

Create a release branch and commit changeset.

## Input
- version: semver string
- phaseNumber: release phase (optional)
- changesetPath: path to changeset file

## Output
Branch name and commit SHA.
